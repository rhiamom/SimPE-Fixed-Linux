<Project>

	<PropertyGroup>
		<!-- Solution-wide version -->
		<Version>8.4.1</Version>
		<FileVersion>8.4.1.0</FileVersion>
		<AssemblyVersion>8.4.1.0</AssemblyVersion>
		<!-- Unified output folder. $(SolutionDir) is only set when MSBuild
		     drives a .sln; building a single .csproj from `dotnet build` or
		     VS Code leaves it as *Undefined*. Fall back to the directory
		     this Directory.Build.targets file lives in (always the solution
		     root) so per-project builds still copy outputs to the unified
		     bin and into the SimPE.Main\Plugins folder. -->
		<SimPeUnifiedOutDir Condition=" '$(SolutionDir)' != '' And '$(SolutionDir)' != '*Undefined*' ">$(SolutionDir)bin\$(Configuration)\</SimPeUnifiedOutDir>
		<SimPeUnifiedOutDir Condition=" '$(SimPeUnifiedOutDir)' == '' ">$(MSBuildThisFileDirectory)bin\$(Configuration)\</SimPeUnifiedOutDir>
		<!-- Suppress CA1416 platform-compatibility warnings — entire solution targets net8.0-windows -->
		<!-- Suppress legacy code warnings in old decompiled/ported code -->
		<NoWarn>$(NoWarn);CA1416;CA2200;CS0067;CS0162;CS0168;CS0169;CS0414;CS0436;CS0618;CS0649;CS8981;SYSLIB0003</NoWarn>
		<!-- Demote MSB3568 (duplicate resource names) and MSB3825 (BinaryFormatter in resx) to messages -->
		<MSBuildWarningsAsMessages>$(MSBuildWarningsAsMessages);MSB3568;MSB3825</MSBuildWarningsAsMessages>
		<!-- Don't generate BinaryFormatter warnings from resx resources -->
		<GenerateResourceWarnOnBinaryFormatterUse>false</GenerateResourceWarnOnBinaryFormatterUse>
	</PropertyGroup>

	<!-- Guard rail: the four PJSE BCON label include packages must exist in
	     the source tree so they can be embedded into pjse.coder.plugin.dll.
	     Without them, the PJSE BCON editor's Label column and TRCN button
	     silently break — no compile error, just missing labels at runtime.
	     Historical precedent: semi-globals was silently dropped once before;
	     see the "recurring class of regression" note in
	     project_directory_build_targets_gaps memory. Failing the build here
	     is louder than waiting for a user bug report.

	     If a file is missing, restore it from
	     SimPe_0_77_69-Source\SimPe Commandline\<name>.package.
	     The csproj declaration is in
	     _PJSE\pjse Coder\SimPe pjse coder.csproj under an ItemGroup that
	     includes each as an EmbeddedResource with an explicit LogicalName
	     ("pjse.Includes.<name>"). The runtime extraction that seeds them
	     to %APPDATA%\SimPe\Data\Plugins\pjse.coder.plugin\Includes\ lives
	     in _PJSE\pjse Coder\FileTable.cs (EnsureIncludesExtracted). -->
	<Target Name="SimPeVerifyPjseIncludes" BeforeTargets="Build">
		<PropertyGroup>
			<_PjseIncludesDir>$(MSBuildThisFileDirectory)_PJSE\pjse Coder\Includes\</_PjseIncludesDir>
		</PropertyGroup>
		<Error Condition=" !Exists('$(_PjseIncludesDir)GLOBALS.package') "
		       Text="Missing PJSE include package: $(_PjseIncludesDir)GLOBALS.package. Restore from SimPe_0_77_69-Source\SimPe Commandline\GLOBALS.package or the BCON editor's label lookup breaks." />
		<Error Condition=" !Exists('$(_PjseIncludesDir)SemiGlobals.package') "
		       Text="Missing PJSE include package: $(_PjseIncludesDir)SemiGlobals.package. Restore from SimPe_0_77_69-Source\SimPe Commandline\SemiGlobals.package or the BCON editor's label lookup breaks." />
		<Error Condition=" !Exists('$(_PjseIncludesDir)Private.package') "
		       Text="Missing PJSE include package: $(_PjseIncludesDir)Private.package. Restore from SimPe_0_77_69-Source\SimPe Commandline\Private.package or the BCON editor's label lookup breaks." />
		<Error Condition=" !Exists('$(_PjseIncludesDir)RelLabels.package') "
		       Text="Missing PJSE include package: $(_PjseIncludesDir)RelLabels.package. Restore from SimPe_0_77_69-Source\SimPe Commandline\RelLabels.package or the BCON editor's label lookup breaks." />
	</Target>

	<Target Name="SimPeCopyToUnifiedBin" AfterTargets="Build" Condition=" '$(SimPeUnifiedOutDir)' != '' ">
		<!-- Ensure destination exists -->
		<MakeDir Directories="$(SimPeUnifiedOutDir)"
				 Condition=" '$(SimPeUnifiedOutDir)' != '' " />

		<!-- Copy main output (DLL or EXE) -->
		<Copy
		  SourceFiles="$(TargetPath)"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="false"
		  Condition="Exists('$(TargetPath)')" />

		<!-- Copy the native EXE host for WinExe/Exe projects -->
		<Copy
		  SourceFiles="$(TargetDir)$(TargetName).exe"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="false"
		  Condition=" '$(OutputType)' != 'Library' And Exists('$(TargetDir)$(TargetName).exe') " />

		<!-- Copy deps.json and runtimeconfig.json for exe projects -->
		<Copy
		  SourceFiles="$(TargetDir)$(TargetName).deps.json"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true"
		  Condition="Exists('$(TargetDir)$(TargetName).deps.json')" />
		<Copy
		  SourceFiles="$(TargetDir)$(TargetName).runtimeconfig.json"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true"
		  Condition="Exists('$(TargetDir)$(TargetName).runtimeconfig.json')" />

		<!-- Copy PDB if it exists -->
		<Copy
		  SourceFiles="$(TargetDir)$(TargetName).pdb"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true"
		  Condition="Exists('$(TargetDir)$(TargetName).pdb')" />

		<!-- Copy XML docs if they exist -->
		<Copy
		  SourceFiles="$(TargetDir)$(TargetName).xml"
		  DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true"
		  Condition="Exists('$(TargetDir)$(TargetName).xml')" />

		<!-- If this is a plugin, wizard, or exporter assembly, also copy to Plugins\
		     subfolder of the unified bin AND to SimPE.Main\bin\$(Configuration)\Plugins\
		     so devs who launch SimPE.Main.exe from its own bin folder see the full set.
		     ExporterLoader, PluginManager, ScannerRegistry, and WizardSelector all scan
		     Plugins\ for these specific *.plugin.dll / *.wizard.dll / *.exporter.dll
		     suffixes — DLLs landing only at the unified-bin root won't be picked up
		     (which is what caused "No exporter plugins available" for mesh export). -->
		<PropertyGroup>
			<_IsPluginOrWizard Condition=" $(AssemblyName.EndsWith('.plugin')) Or $(AssemblyName.EndsWith('.wizard')) Or $(AssemblyName.EndsWith('.exporter')) ">true</_IsPluginOrWizard>
			<_SimPeMainBin Condition=" '$(SolutionDir)' != '' And '$(SolutionDir)' != '*Undefined*' ">$(SolutionDir)SimPE.Main\bin\$(Configuration)\</_SimPeMainBin>
			<_SimPeMainBin Condition=" '$(_SimPeMainBin)' == '' ">$(MSBuildThisFileDirectory)SimPE.Main\bin\$(Configuration)\</_SimPeMainBin>
		</PropertyGroup>

		<MakeDir Directories="$(SimPeUnifiedOutDir)Plugins" Condition=" '$(_IsPluginOrWizard)' == 'true' " />
		<Copy SourceFiles="$(TargetPath)" DestinationFolder="$(SimPeUnifiedOutDir)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And Exists('$(TargetPath)')" />
		<Copy SourceFiles="$(TargetDir)$(TargetName).pdb" DestinationFolder="$(SimPeUnifiedOutDir)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And Exists('$(TargetDir)$(TargetName).pdb')" />

		<MakeDir Directories="$(_SimPeMainBin)Plugins" Condition=" '$(_IsPluginOrWizard)' == 'true' And '$(_SimPeMainBin)' != '' " />
		<Copy SourceFiles="$(TargetPath)" DestinationFolder="$(_SimPeMainBin)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And '$(_SimPeMainBin)' != '' And Exists('$(TargetPath)')" />
		<Copy SourceFiles="$(TargetDir)$(TargetName).pdb" DestinationFolder="$(_SimPeMainBin)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And '$(_SimPeMainBin)' != '' And Exists('$(TargetDir)$(TargetName).pdb')" />

		<!-- Mirror the plugin's transitive DLL dependencies into Plugins\ next
		     to the plugin DLL. Symptom this fixes: when a plugin references a
		     non-plugin library that's NOT also referenced by SimPE.Main (e.g.
		     simpe.ngbh.scanner.plugin needs simpe.toolbox.scanner, but SimPE.Main
		     doesn't), .NET 8's Assembly.LoadFrom resolver for that plugin probes
		     the plugin's own directory and the host's deps.json — NOT the parent
		     bin\$(Configuration)\ folder. The DLL sits at bin\Release\ but
		     stays invisible to the plugin. ReflectionTypeLoadException with
		     "Could not load file or assembly 'simpe.X, Version=8.2.14.0'" was
		     the silent failure that hid this from us until we added pluginlog.txt
		     failure logging. Other plugin's *.plugin.dll outputs are excluded so
		     plugins don't pull each other's binaries into their own folder. -->
		<ItemGroup Condition=" '$(_IsPluginOrWizard)' == 'true' ">
			<_SimPePluginDeps Include="$(TargetDir)*.dll"
			  Exclude="$(TargetDir)$(TargetName).dll;$(TargetDir)*.plugin.dll;$(TargetDir)*.wizard.dll;$(TargetDir)*.exporter.dll" />
		</ItemGroup>
		<Copy SourceFiles="@(_SimPePluginDeps)" DestinationFolder="$(SimPeUnifiedOutDir)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And '@(_SimPePluginDeps)' != '' " />
		<Copy SourceFiles="@(_SimPePluginDeps)" DestinationFolder="$(_SimPeMainBin)Plugins"
		  SkipUnchangedFiles="true" Condition=" '$(_IsPluginOrWizard)' == 'true' And '$(_SimPeMainBin)' != '' And '@(_SimPePluginDeps)' != '' " />

		<!-- Plugin projects can ship sidecar data files (e.g. PJSE Coder's
		     GlobalStrings.package, which is referenced as
		     <None Include="pjse.coder.plugin\GlobalStrings.package"> with a
		     TargetPath of "Plugins\pjse.coder.plugin\GlobalStrings.package").
		     The CopyToOutputDirectory rule deposits them under
		     $(TargetDir)Plugins\<plugin-name>\* — but only the plugin DLL was
		     being copied to the unified bin's Plugins\ folder, leaving the
		     data files behind. Mirror the entire Plugins\ tree so any sidecar
		     content travels with its plugin. -->
		<ItemGroup>
			<_SimPePluginSidecars Include="$(TargetDir)Plugins\**\*.*" />
		</ItemGroup>
		<Copy SourceFiles="@(_SimPePluginSidecars)"
		  DestinationFiles="@(_SimPePluginSidecars->'$(SimPeUnifiedOutDir)Plugins\%(RecursiveDir)%(Filename)%(Extension)')"
		  SkipUnchangedFiles="true"
		  Condition=" '@(_SimPePluginSidecars)' != '' " />
		<Copy SourceFiles="@(_SimPePluginSidecars)"
		  DestinationFiles="@(_SimPePluginSidecars->'$(_SimPeMainBin)Plugins\%(RecursiveDir)%(Filename)%(Extension)')"
		  SkipUnchangedFiles="true"
		  Condition=" '@(_SimPePluginSidecars)' != '' And '$(_SimPeMainBin)' != '' " />

		<!-- A plugin can also ship a sibling data/ folder (e.g. Collection
		     Creator's MaxisObjectList.txt and Sample Icons/, declared as
		     <None Include="data\**\*"> with TargetPath data\... ). The
		     CopyToOutputDirectory rule puts them at $(TargetDir)data\* but,
		     like the Plugins\ sidecars above, nothing was promoting them to
		     the unified-bin location next to where the plugin DLL lands.
		     Plugins that look up "data next to my DLL" therefore found
		     nothing once SimPE.Main ran from the unified bin. Mirror data\
		     into Plugins\data\ at both unified destinations so the lookup
		     finds the same files no matter which bin SimPE launches from. -->
		<ItemGroup Condition=" '$(_IsPluginOrWizard)' == 'true' ">
			<_SimPePluginData Include="$(TargetDir)data\**\*.*" />
		</ItemGroup>
		<Copy SourceFiles="@(_SimPePluginData)"
		  DestinationFiles="@(_SimPePluginData->'$(SimPeUnifiedOutDir)Plugins\data\%(RecursiveDir)%(Filename)%(Extension)')"
		  SkipUnchangedFiles="true"
		  Condition=" '$(_IsPluginOrWizard)' == 'true' And '@(_SimPePluginData)' != '' " />
		<Copy SourceFiles="@(_SimPePluginData)"
		  DestinationFiles="@(_SimPePluginData->'$(_SimPeMainBin)Plugins\data\%(RecursiveDir)%(Filename)%(Extension)')"
		  SkipUnchangedFiles="true"
		  Condition=" '$(_IsPluginOrWizard)' == 'true' And '$(_SimPeMainBin)' != '' And '@(_SimPePluginData)' != '' " />

		<!-- For EXE/WinExe projects (SimPE.Main, Wizards of SimPe), also copy every
		     transitive runtime dependency (third-party DLLs, *.config, the runtimes\
		     native folder) from the project's bin into the unified bin. Without this,
		     bin\$(Configuration)\SimPE.Main.exe ships without WeifenLuo.WinFormsUI.Docking.dll
		     and the OpenTK assemblies and crashes at startup with FileNotFoundException
		     on a clean install (the per-project bin has them, but the unified one didn't).
		     Plugin DLLs are excluded — they live under Plugins\ and the dedicated copy
		     above already handles them. -->
		<ItemGroup Condition=" '$(OutputType)' != 'Library' ">
			<_SimPeAppDeps Include="$(TargetDir)*.dll" Exclude="$(TargetDir)*.plugin.dll;$(TargetDir)*.wizard.dll" />
			<_SimPeAppConfigs Include="$(TargetDir)*.config" />
			<_SimPeAppRuntimes Include="$(TargetDir)runtimes\**\*.*" />
		</ItemGroup>
		<Copy SourceFiles="@(_SimPeAppDeps)" DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true" Condition=" '$(OutputType)' != 'Library' " />
		<Copy SourceFiles="@(_SimPeAppConfigs)" DestinationFolder="$(SimPeUnifiedOutDir)"
		  SkipUnchangedFiles="true" Condition=" '$(OutputType)' != 'Library' " />
		<Copy SourceFiles="@(_SimPeAppRuntimes)"
		  DestinationFiles="@(_SimPeAppRuntimes->'$(SimPeUnifiedOutDir)runtimes\%(RecursiveDir)%(Filename)%(Extension)')"
		  SkipUnchangedFiles="true" Condition=" '$(OutputType)' != 'Library' " />

	</Target>

</Project>


